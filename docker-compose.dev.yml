version: '3.8'

services:
  web:
    volumes:
      - ./whisper_video_to_text:/app/whisper_video_to_text:ro
      - ./tests:/app/tests:ro
    environment:
      - RELOAD=true
    command: ["uvicorn", "whisper_video_to_text.web.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
